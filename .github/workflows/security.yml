name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly Monday 08:00 UTC catches new advisories on a stable branch
    # even if no PRs land that week.
    - cron: '0 8 * * 1'

permissions:
  contents: read
  security-events: write
  pull-requests: write

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  socket:
    name: Socket.dev supply-chain scan
    runs-on: ubuntu-latest
    # Hoist the secret to the job env so step-level `if: env.X != ''`
    # actually sees it. (`env:` defined inside a single step isn't visible
    # to that step's own `if:` — GitHub evaluates `if` before binding env.)
    env:
      SOCKET_SECURITY_API_KEY: ${{ secrets.SOCKET_SECURITY_API_KEY }}
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      # `SocketDev/socket-security-action` was deleted upstream — the repo 404s.
      # GitHub resolves every `uses:` ref during "Prepare all required actions",
      # before any step `if:` is evaluated, so the dead ref failed this job even
      # with the secret unset, and broke the weekly Dependabot run too
      # (`git_dependencies_not_reachable`). Drive the maintained CLI instead;
      # `socket ci` is an alias for `socket scan create --report`.
      - name: Run Socket scan
        if: env.SOCKET_SECURITY_API_KEY != ''
        continue-on-error: true # report-only, don't block PRs
        env:
          SOCKET_SECURITY_API_TOKEN: ${{ secrets.SOCKET_SECURITY_API_KEY }}
        run: bunx socket ci
      - name: Skip notice
        if: env.SOCKET_SECURITY_API_KEY == ''
        run: echo "::notice::SOCKET_SECURITY_API_KEY not configured — skipping Socket.dev scan"

  snyk:
    name: Snyk vulnerability scan
    runs-on: ubuntu-latest
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - run: bun install --frozen-lockfile
      - name: Snyk test
        if: env.SNYK_TOKEN != ''
        uses: snyk/actions/node@master
        continue-on-error: true # report-only, don't block PRs
        with:
          args: --severity-threshold=high --sarif-file-output=snyk.sarif
      - name: Upload Snyk SARIF to GitHub Security
        if: env.SNYK_TOKEN != '' && hashFiles('snyk.sarif') != ''
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk.sarif
      - name: Skip notice
        if: env.SNYK_TOKEN == ''
        run: echo "::notice::SNYK_TOKEN not configured — skipping Snyk scan"

  npm-audit:
    name: npm audit (always-on baseline)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - run: bun install --frozen-lockfile
      # bun audit was added in 1.2; warn-only so PRs don't gate on it.
      - name: bun audit (high)
        run: bun audit --audit-level=high || echo "::warning::bun audit reported issues"

  codeql:
    name: CodeQL JavaScript/TypeScript
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
          queries: security-and-quality
      - uses: github/codeql-action/analyze@v4
        with:
          category: '/language:javascript-typescript'
